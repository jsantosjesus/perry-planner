export const CadastrarModal = ({fechar}) =>{
    return(
        <div className="bodyEntrarModal">
            <p>cadastrar</p>
            <button onClick={fechar}>Fechar</button>
        </div>
    )
}